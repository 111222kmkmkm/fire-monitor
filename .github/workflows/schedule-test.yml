name: Schedule Test

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: '3,13,23,33,43,53 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Print UTC time
        run: date -u
